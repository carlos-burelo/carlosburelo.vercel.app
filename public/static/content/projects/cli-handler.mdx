---
name: CLI Handler
tags:
  - cli
  - command line parser
  - handlers
description: Parser de argumentos de linea de comandos con funciones desencadenadas
image: /static/images/projects/cli-handler.webp
stack:
  - name: typescript
    percent: 100
---

# Intruduction

CLI Handler is a simple terminal app of Command Line Interface apps with event handlers for match pattern in some context, for example:

```js

const Node = {
  name: 'g'
  handler: (name, index)=> console.log('Hello world')
}
```
